{
  "name": "runway-ios",
  "version": "0.1.0",
  "private": true,
  "description": "Native iOS shell for Runway (Capacitor). Loads the live web app.",
  "scripts": {
    "add-ios": "cap add ios",
    "sync": "cap sync",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
